server:
  port: 8880

spring:
  application:
    name: admin-server
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config-server

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8010/eureka


